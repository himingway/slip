module gen_for_test #(param N = 4) (clk, rst_n, data_in, data_out) {
    logic [7:0] data_in;
    logic [7:0] data_out;

    `for (`i = 0; `i < N; `i = `i + 1) {
        logic [7:0] data_`i;
        assign data_`i = data_in + `i;
    }

    assign data_out = data_3;
}
